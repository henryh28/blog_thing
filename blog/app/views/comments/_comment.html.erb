<p>
  <%= comment.commenter %> posted on <%= comment.created_at.strftime("%b %e, %y at %r") %>:

  <%= link_to "Delete Comment", [comment.article, comment], method: :delete, data: { confirm: "Delete comment?" } %><br>

  <%= comment.content %>
</p>
